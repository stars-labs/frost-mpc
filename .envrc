watch_file ./flake.nix ./flake.lock
# Enable flakes + nix-command so `use flake` below actually works on
# nix installs that haven't opted in via nix.conf yet.
export NIX_CONFIG="experimental-features = nix-command flakes"
use flake .
